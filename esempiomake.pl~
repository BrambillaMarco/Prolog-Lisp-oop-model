make(Class, Instance) :-
    class(Class, _Parents), % Verifica se la classe è definita
    assert(instance(Instance, Class)).

% Esempio di utilizzo
make(persona, *persona1*),
make(studente, *studente1*),
make(impiegato, *impiegato1*).

% Ora puoi utilizzare le istanze create nelle regole e nei fatti successivi
