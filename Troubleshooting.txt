GENERALE

1) gestire i parametri nei metodi passati alla def_class

2) richiamare i predicati is_class e is_istance ove necessario

3) inserire controlli generici sul tipo di attributi passati

3) capire dove inserire dei cut e dove no. (nei casi base)


CONTROLLI FINALI

1) non posso passare due volte una classe parent nella def_class, sennò aggiungerei due volte tutti i fields e methods
   codice: 
   \+ member(Parent, Parents),  % Controllo per evitare loop

2) controllare cosa succede se nel corpo di un metodo passato alla def_class ci sono
   delle istruzioni non lecite in prolog

3) debug vario a prova antoniotti
   
