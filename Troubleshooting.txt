def-class

1) risolvere il fatto che si possano creare fields e methods identici
2) usare retract in caso sia stato fatto un assert di una classe che per qualche motivo doveva fallire

make

1) l'input utente della traccia per quanto riguarda i campi di un istanza è problematico